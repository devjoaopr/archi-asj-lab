# archi-asj-lab
A large micro-service strucure API, made for learning aand applying new technologies. 
if wanto to run and check it out, you just need to run docker compose inside the "infra" module, it runs the whole application.
make sure to have JDK25 downloaded and docker, otherwise, you will not be able to run. 

