server:
  port: 8084

spring:
  application:
    name: case-service

  datasource:
    url: jdbc:postgresql://localhost:5436/casedb
    username: joao
    password: joaodev

  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.xml

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true